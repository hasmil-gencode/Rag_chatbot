#!/bin/bash

echo "🧹 Complete Docker Cleanup & Fresh Deployment Guide"
echo "=================================================="
echo ""
echo "Run these commands on your server:"
echo ""
echo "# 1️⃣ Stop and remove old containers"
echo "cd /path/to/old/Rag_chatbot"
echo "docker compose down"
echo ""
echo "# 2️⃣ Remove old containers, images, and volumes (OPTIONAL - if want complete cleanup)"
echo "docker compose down -v --rmi all"
echo ""
echo "# 3️⃣ List all containers (verify stopped)"
echo "docker ps -a"
echo ""
echo "# 4️⃣ Remove specific containers if still exist (replace with actual names)"
echo "docker rm -f rag_chatbot-rag-chatbot-ui-1"
echo "docker rm -f container_name_here"
echo ""
echo "# 5️⃣ Remove old images (OPTIONAL)"
echo "docker images | grep rag_chatbot"
echo "docker rmi image_id_here"
echo ""
echo "# 6️⃣ Clean up unused Docker resources"
echo "docker system prune -a --volumes"
echo "# WARNING: This removes ALL unused containers, images, networks, and volumes!"
echo ""
echo "# 7️⃣ Remove old project folder (BACKUP FIRST!)"
echo "cd /path/to"
echo "mv Rag_chatbot Rag_chatbot_backup_$(date +%Y%m%d)"
echo ""
echo "# 8️⃣ Extract new project"
echo "tar -xzf rag_chatbot_prod.tar.gz"
echo "cd Rag_chatbot"
echo ""
echo "# 9️⃣ Update .env file"
echo "nano .env"
echo "# Change: MONGODB_URI=...ragchatbot_stag → ...ragchatbot_prod"
echo ""
echo "# 🔟 Deploy fresh"
echo "docker compose up -d --build"
echo ""
echo "# ✅ Verify running"
echo "docker ps"
echo "docker logs rag_chatbot-rag-chatbot-ui-1"
echo ""
echo "=================================================="
echo ""
echo "⚠️  SAFE CLEANUP (Recommended):"
echo "   docker compose down"
echo "   # Keep old folder as backup"
echo "   # Deploy new in different location"
echo ""
echo "🔥 COMPLETE CLEANUP (Nuclear option):"
echo "   docker compose down -v --rmi all"
echo "   docker system prune -a --volumes"
echo "   rm -rf old_folder"
echo ""
