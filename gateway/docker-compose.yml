version: '3.8'
services:
  gateway:
    build: .
    ports:
      - "4000:4000"
    env_file: .env
    depends_on:
      - mongodb
    restart: unless-stopped

  mongodb:
    image: mongo:7
    volumes:
      - gateway_mongo_data:/data/db
    restart: unless-stopped

volumes:
  gateway_mongo_data:
